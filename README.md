Running the project:

This site is deployed on github pages: (url here).

If a local instance is desired follow these instructions:

1. Unzip the file
2. Ensure npm or pnpm is installed
3. In the terminal instance of the enclosing folder, run
    pnpm install
    pnpm run dev

and click on the localhost link in the CLI.